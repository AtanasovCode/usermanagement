import React, { useState } from "react";
import {
    View,
    Text,
} from "react-native";

const Homepage = ({ navigation }) => {
    return (
        <View>
            <Text>
                Home
            </Text>
        </View>
    );
}

export default Homepage;